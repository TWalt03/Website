const form = document.getElementById('form')
const user_input = document.getElementById('user-in')
const email_input = document.getElementById('email-in')
const pass_input = document.getElementById('pass-in')
const repass_input = document.getElementById('newPass-in')

form.addEventListener('submit', (e)=>{

    
})